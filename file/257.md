---
title: 发布你自己的npm模块
date: 2017-12-07 22:09:10
tags: npm模块
---

准备工作：Mac OS X terminal，或Linux，或Windows ，安装Node.js

1 https://www.npmjs.com/ 注册账号。

2.https://github.com/ 注册账号， https://help.github.com/ 设置好git环境。

3.打开Terminal，运行

        $ npm login
        输入npm的用户名，密码，email


4.为自己的模块想一个名字，然后运行

        $ npm view some-name
        如果返回404，说明这个名字没被占用，本文用flash-node-npm-module-shaopeng为例



5.在github上创建一个project，以模块的名字（flash-node-npm-module-shaopeng）命名。注意以下.gitignore选Node，license可以随便选一个。



6.从github上将项目同步下来


    $ git clone <your-git-repo-url>


7.切换到项目的目录中

      $ cd <your-git-repo-dir>
    
8.创建npm模块，
    
       $ npm init
       按回车键可以直接接受默认值，我只修改了version为0.0.1，然后添加了description和license，这些以后都可以改。完成之后生成了package.json。



9.在package.json平级的目录中，创建一个文件index.js

        $ touch index.js
        查看目录中的所有文件，用 $ ls -la
        $ ls -la 

10.编辑index.js，这就是你的模块的主逻辑：

        $ nano index.js
        
11.至此大部分代码工作就完成了，用git查看一下状态：

        $ git status
        所以其实除去git创建时自带的配置文件和版权文件等，我们只创建了两个文件：package.json 和 index.js
    

12.添加这两个文件到git中，然后同步到github

        $ git add index.js package.json
        $ git commit -m "npm module, initial logic"
        $ git push origin master

13.激动人心的一刻来到了，发布到npm中

        $ npm publish

14.验证一下是不是发布了，

         $ npm view <your-module-name>

15.用自己刚发布的模块试试手吧！创建一个本地项目（想同步到github也可以，本文略过）
    
        $ cd ../
        $ mkdir test-my-module
        $ cd test-my-module

16.创建一个npm project，使用上文中的npm init，可以按回车选择所有默认值

        $ npm init
    
17.安装我们刚刚发布的模块

        $ npm install --save <your-module-name>
        注意上面的 --save 选项，它会将被安装的模块添加到当前目录的package.json中。可以查看一下package.json文件确认。
        $ cat package.json

18.创建一个js文件，如果只是测试可以是任何名字，比如app.js

        $ touch app.js
        $ nano app.js
        // 以下是app.js的内容
        var addFn = require('flash-node-npm-module-shaopeng');
        var add30 = addFn(30);
        var result = add30(50);
        
        console.log(result); // should be 80

19.用Node运行app.js，查看结果

        $ node app.js
        $ 80

20.现在你已经掌握了最基本的创建，编写，以及发布npm模块的基本技能了。我们来回顾一下需要掌握的主要的命令：
    
        $ npm login
         登录npm账号，为后面发布做准备
    
        $ npm view <some-name>
        按名称查找npm模块
    
        $ npm init
        在当前目录中创建一个npm项目，本质上是创建package.json文件
    
        $ npm publish
        发布npm模块 
    
        $ npm install --save <npm-module-name>
        安装一个npm模块，并添加到package.json中
    
         下面是常用的Terminal命令
        $ cat filename   # 显示文件内容
        $ ls -la         # 列出当前目录中所有文件
        $ mkdir dirname  # 创建一个目录
        $ cd dirname     # 切换到dirname目录下
        $ touch filename # 创建一个空文件
        $ nano filename  # Mac OS 下的文件编辑器，也可以使用vim, emacs, 记事本等 
        
        我刚发布的一个npm模块：oopstool